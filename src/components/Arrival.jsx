import SectionTitle from "./SectionTitle";

const Arrival = () => {
      return (
            <div className="mt-[140px]">
                 <SectionTitle heading={''} subHeading={'Featured'} titleHeading={'New Arrival'}/> 

                 
            </div>
      );
};

export default Arrival;